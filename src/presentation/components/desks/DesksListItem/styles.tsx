import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderRadius: 10,
    flexDirection: 'row',
    gap: 10,
    margin: 10,
    paddingHorizontal: 10
  }
});
