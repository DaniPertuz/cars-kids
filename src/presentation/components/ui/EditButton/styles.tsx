import { StyleSheet } from 'react-native';
import { globalColors } from '../../../theme/globalColors';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: globalColors.darkLight,
    borderRadius: 100,
    padding: 5
  }
});
