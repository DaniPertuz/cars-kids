import { StyleSheet } from 'react-native';

export const authStyles = StyleSheet.create({
  formContainer: {
    gap: 10,
    marginTop: 20
  },
  imageContainer: {
    alignItems: 'center',
    marginBottom: 30
  },
  mainImage: {
    height: 200,
    width: 300
  }
});
