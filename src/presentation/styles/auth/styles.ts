import { StyleSheet } from 'react-native';

export const authStyles = StyleSheet.create({
  formContainer: {
    gap: 15,
    marginTop: 20
  },
  imageContainer: {
    alignItems: 'center'
  },
  mainImage: {
    height: 200,
    width: 300
  }
});
